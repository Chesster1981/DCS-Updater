# Discord bot token — NEVER commit the real value
# Create at https://discord.com/developers/applications → Bot → Reset Token
DISCORD_BOT_TOKEN=

# GitHub personal access token — improves api.github.com rate limits
# Fine-grained or classic token with read access to the DCS-Updater repo
GITHUB_TOKEN=
